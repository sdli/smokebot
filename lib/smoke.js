/**
 * In this file,
 * we need to check the packages and folders to make sure `smokebot` can be installed.
 * Firstly, the `package.json` file must have the default settings, like:
 * 1. Static folder(s)
 * 2. Proxy Setting(s) 
 */

